version: '3'

services:

  todotests:
    image: todotests
    depends_on:
      - todofront
      - todoback
    environment:
      - FRONT_HOST=todofront
    restart: always

  todofront:
    image: todofrontend
    depends_on:
      - todoback
    expose:
      - 3000:3000
    environment:
      - REACT_APP_API_HOST=todoback
    restart: always

  todoback:
    image: todobackend
    expose:
      - 3001:3001
    restart: always
